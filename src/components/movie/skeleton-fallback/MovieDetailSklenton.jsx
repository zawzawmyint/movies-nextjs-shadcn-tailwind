import { Skeleton } from "@/components/ui/skeleton";

const MovieDetailSkelenton = () => {
  return (
    <>
      <Skeleton className={"h-screen w-full"} />
    </>
  );
};

export default MovieDetailSkelenton;
